import OrganiserDashboard from './OrganiserDashboard';

export const metadata = {
  title: 'Organiser Hub - HealthSummits.tv',
};

export default function OrganiserHubPage() {
  return <OrganiserDashboard />;
}
